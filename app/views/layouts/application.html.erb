<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title ? @page_title : 'Welcome' %> | Project DE</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <header>
    <div class="container">
      <h2>Project DE <small><%= @page_title ? @page_title : 'Welcome' %></small></h2>
    </div>
  </header>
  <div class="navbar navbar-default navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="<%= params[:controller] == 'home' ? 'active' : '' %>"><%= link_to 'Home' , root_path %></li>
          <li class="<%= params[:controller] == 'complaint' ? 'active' : '' %>"><%= link_to 'Register Complaint' , complaints_path %></li>
          <li class="<%= params[:controller] == 'about' ? 'active' : '' %>"><%= link_to 'About' , about_index_path %></li>
          <li class="<%= params[:controller] == 'contact' ? 'active' : '' %>"><%= link_to 'Contact Us' , contact_index_path %></li>
        </ul>
        <div class="dropdown navbar-right">
          <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
            <%= user_signed_in? ? current_user.email: 'Guest' %>
            <span class="caret"></span>
          </button>
          <ul class="dropdown-menu navbar-right">
            <% if user_signed_in? %>
                <li class="dropdown-header"><%= 1==1? 'Logged in as UNAME' : 'As a Guest' %></li>
                <li><%= link_to 'Profile', edit_user_registration_path %></li>
                <li><%= link_to 'Settings', edit_user_registration_path %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
            <% else %>
                <li class="<%= params[:controller] == 'sessions' ? 'active' : '' %>"><%= link_to 'Login' , new_user_session_path %></li>
                <li class="<%= params[:controller] == 'sessions' ? 'active' : '' %>"><%= link_to 'Admin Area' , new_admin_session_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <section>
    <div class="container">
      <div class="row">
        <% if notice %><div class="alert alert-info"><%= notice %></div><% end %>
        <% if alert %><div class="alert alert-warning"><%= alert %></div><% end %>
        <div class="col-md-8"><%= yield %></div>
        <div class="col-lg-4"> <%= render 'partials/sidebar' %></div>
      </div>

    </div>
  </section>

  <div class="navbar navbar-fixed-bottom">
    <footer><p>&copy  <%= link_to 'Copyright 2017 ', 'https://github.com/piyush-multiplexer/projectde' %> | All Rights Reserved.</p></footer>
  </div>

  </body>
</html>
