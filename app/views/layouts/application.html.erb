<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title ? @page_title : 'Welcome' %> | Project DE</title>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="navbar navbar-default navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <div id="navbar" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="<%= params[:controller] == 'home' ? 'active' : '' %>"><%= link_to 'Home' , root_path %></li>
          <li class="disabled"><%= link_to 'Register Complaint' , '#' %></li>
          <li class="<%= params[:controller] == 'about' ? 'active' : '' %>"><%= link_to 'About' , about_path %></li>
          <li class="<%= params[:controller] == 'contactus' ? 'active' : '' %>"><%= link_to 'Contact Us' , contactus_path %></li>
        </ul>
        <div class="dropdown navbar-right">
          <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
            <%= logged_in? ? 'Admin' : 'Guest' %>
            <span class="caret"></span>
          </button>
          <ul class="dropdown-menu">
            <% if logged_in? %>
                <li class="dropdown-header"><%= logged_in? ? 'Logged in as Admin' : 'As a Guest' %></li>
                <li><%= link_to 'Profile', current_user %></li>
                <li><%= link_to 'Settings', '#' %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Log out', logout_path, method: 'delete' %></li>
            <% else %>
                <li class="<%= params[:controller] == 'sessions' ? 'active' : '' %>"><%= link_to 'Login' , login_path %></li>
            <% end %>
          </ul>
        </div>
      </div>

    </div>

  </div>
  <header>
    <div class="container">
      <h2><div id="timestamp"><%= Time.now %></div>Project DE <small><%= @page_title ? @page_title : 'Welcome' %></small></h2>
    </div>
  </header>
  <section>
    <div class="container">
      <div class="row">
        <% flash.each do |message_type, message| %>
            <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
      <% end %>
        <div class="col-md-8"><%= yield %></div>
        <div class="col-lg-4"> <%= render 'partials/sidebar' %></div>
      </div>

    </div>
  </section>

  <div class="navbar navbar-fixed-bottom">
    <footer><p>&copy Copyright 2017 | All Rights Reserved.</p></footer>
  </div>

  </body>
</html>
