
  <% if step != 'userinfo' %>
    <%= link_to 'Previous step', previous_wizard_path , class: 'btn btn-primary'%>
  <% end %>
  <%= f.submit next_wizard_path ? 'finish' : 'next', class: step == 'description' ? 'btn btn-success' : 'btn btn-primary' %>
