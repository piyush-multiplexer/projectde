
  <% if previous_step.present? %>
    <%= link_to 'Previous step', edit_previous_step_path , class: 'btn btn-primary'%>
  <% end %>
  <%= f.submit next_step.present? ? 'next' : 'finish', class: current_step == 'description' ? 'btn btn-success' : 'btn btn-primary' %>